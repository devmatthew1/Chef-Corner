*Copy the folder into ur htdoc folder for xampp or www folder for wamp
*Change the base url to the root folder 
----Go to line 34 of \application\config\config.php
---- Looks like http://localhost/root folder/
*Your phpMyadmin login username is root and password leave empty
*Create a database name it "chef" 
*Inside the DB "chef", import the chef.sql
*Go to the webpage http://localhost/root folder/ and login
*The login email is "admin@admin.com"and password is "password"